<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TimeCAlling" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:statusBarColor">#262626</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TimeCAlling" parent="Base.Theme.TimeCAlling" />

    <!-- 캘린더의 날짜(Day)의 스타일 설정 -->
    <style name="CalenderViewCustom" parent="Theme.AppCompat">
        <item name="android:textColor">@drawable/sel_calender_textcolor</item>
        <item name="android:textAppearance">@style/TextAppearance.TimeCAlling.Calender</item>
    </style>

    <!-- 캘린더의 날짜(Day)의 스타일 설정 -->
    <style name="CalenderViewDateCustomText" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textColor">@drawable/sel_calender_textcolor</item>
        <item name="android:textAppearance">@style/TextAppearance.TimeCAlling.Calender</item>
    </style>
</resources>